#include "Sprite.h"

// AJ_Texture::AJ_Texture(const TextureID &id) {
//     // texture = GameClass::GetInstance().Assets->GetTexture(id);
//     if (!texture) {
//             SDL_LogError(SDL_LOG_CATEGORY_APPLICATION, "Failed to create AJ_Texture: TextureID:: %s, we put an empty texture instead.\n", id);
//     }
// }
// AJ_Texture::~AJ_Texture() {
//     if (texture) {
//         SDL_DestroyTexture(texture);
//         texture = nullptr;
//     }
// }